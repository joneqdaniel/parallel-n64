fixture_id: paper-mario-title-block-family-probe
game: paper-mario
phase: phase-1
mode_scope:
  - off
  - on
authority:
  type: authoritative-savestate
  base_fixture: paper-mario-title-screen
target:
  description: Snapshot the remaining title `2048x1 fs515` authored-RDRAM loadblock family directly from RDRAM.
runtime_rules:
  internal_scale: 4x
  serial_execution: true
  display_required: true
evidence:
  required:
    - screenshot
    - retroarch_log
    - semantic_trace
    - hires_evidence
    - block_family_probe_plan
    - block_family_probe_span
    - block_family_probe_report
